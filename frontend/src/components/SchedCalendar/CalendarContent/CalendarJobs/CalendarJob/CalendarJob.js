import React from 'react';
import Aux from '../../../../../hoc/Aux/Aux';

const calendarJob = (props) => (
       <Aux>
            {props.name}
            {props.address}
       </Aux>
);

export default calendarJob;