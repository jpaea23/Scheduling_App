import React, { Component } from "react"
import Aux from "../../hoc/Aux/Aux";

class Scheduler extends Component{
    render(){
        return(
            <Aux>
                Box of one date that returns all the data from that day in descending order...
            </Aux>
        );
    }
}

export default Scheduler;